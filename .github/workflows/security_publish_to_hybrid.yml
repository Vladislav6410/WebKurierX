name: 🔐 Security Telemetry → Hybrid (v3)

on:
  push:
    paths:
      - "labs/**/.ci/security_report.json"
    branches:
      - main
      - dev
  workflow_dispatch:

jobs:
  publish-security-telemetry:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 🔧 Install dependencies
        run: pip install requests pyyaml

      - name: 🚀 Publish security reports to Hybrid mock API
        env:
          HYBRID_ENDPOINT: ${{ secrets.HYBRID_SECURITY_ENDPOINT }}
          HYBRID_TOKEN: ${{ secrets.HYBRID_API_TOKEN }}
        run: |
          echo "🔎 Searching for security reports..."
          python - <<'PYCODE'
import json, os, requests, pathlib, datetime

endpoint = os.getenv("HYBRID_ENDPOINT", "http://127.0.0.1:9090/api/v1/security/report")
token = os.getenv("HYBRID_TOKEN", "mock-token")
headers = {"Authorization": f"Bearer {token}", "Content-Type": "application/json"}

reports = list(pathlib.Path("labs").rglob("security_report.json"))
summary = {"sent": 0, "failed": 0, "total": len(reports)}

if not reports:
    print("❌ No security reports found.")
    raise SystemExit(0)

for path in reports:
    lab = path.parts[1] if len(path.parts) > 1 else "unknown"
    with open(path, "r", encoding="utf-8") as f:
        try:
            data = json.load(f)
        except json.JSONDecodeError:
            print(f"⚠️ Invalid JSON in {path}")
            summary["failed"] += 1
            continue

    payload = {
        "lab": lab,
        "repository": "WebKurierX",
        "timestamp": data.get("timestamp", datetime.datetime.utcnow().isoformat()),
        "issues": data.get("issues", 0),
        "severity": data.get("severity", {}),
        "risk_score": data.get("risk_score", 0.0),
        "source_path": str(path),
    }

    try:
        r = requests.post(endpoint, headers=headers, json=payload, timeout=10)
        if r.status_code == 200:
            print(f"✅ Sent {lab}: status={r.status_code}")
            summary["sent"] += 1
        else:
            print(f"⚠️ {lab}: unexpected status {r.status_code}")
            summary["failed"] += 1
    except Exception as e:
        print(f"❌ Failed to send {lab}: {e}")
        summary["failed"] += 1

print("\n📊 Security Telemetry Summary:")
print(json.dumps(summary, indent=2, ensure_ascii=False))
if summary["failed"]:
    raise SystemExit(f"⚠️ One or more telemetry sends failed ({summary['failed']}).")
else:
    print("✅ All telemetry reports sent successfully!")
PYCODE

      - name: 🧾 Summary
        if: always()
        run: |
          echo "✅ Security telemetry transfer complete."
          echo "📦 Reports transmitted to Hybrid mock endpoint."
          echo "💡 Check 'Actions' logs for details per lab."
